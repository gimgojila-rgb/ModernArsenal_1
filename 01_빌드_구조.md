# ModernArsenal 인수인계 01: 빌드와 구조 (v0.4.2)

> 여러 파일로 나눠 둔 인수인계서 중 하나예요. 전체 목록은 `00_먼저_읽기.md`에 있어요.

## 2. 파일 위치

| 무엇 | 위치 |
|---|---|
| 모드 소스 (실제 작업본) | `C:\Users\SUN\Documents\My Games\Terraria\tModLoader\ModSources\ModernArsenal\` |
| tModLoader 설치 경로 | `E:\SteamLibrary\steamapps\common\tModLoader\` |
| 게임 로그 | `E:\SteamLibrary\steamapps\common\tModLoader\tModLoader-Logs\client.log` |
| 빌드 결과물 | `Documents\My Games\Terraria\tModLoader\Mods\ModernArsenal.tmod` |
| 에셋 생성 스크립트 모음 | `ModSources\ModernArsenal_tools.zip` |
| 패턴 미리보기 | `ModSources\Apache_AllPatterns.gif`, `Apache_AllPatterns.mp4` (소리 포함), `Apache_Closeup_States.gif` |
| M4A1 미리보기 | `m4a1_fire.gif` (실제 속도), `m4a1_closeup.gif` (0.2배속), `m4a1_sprite.png` (스프라이트 시트) |

- tModLoader 버전은 `2026.7.3.0`이에요. 1.4.4 계열이고 컴파일 상수는 `TML_2026_07`이에요.
- 사용자 PC는 Windows예요. Cowork 세션에서는 `ModSources` 폴더랑 tModLoader 설치 폴더를 연결해서 썼어요. **폴더 접근 권한은 세션마다 새로 받아야 해요.**
- `ModSources` 안에 있는 `Claude outputs` 폴더는 앱이 채팅 첨부파일을 자동으로 저장하면서 생긴 거예요. 모드가 아니라서 tModLoader가 "공백 있는 폴더" 경고를 띄우는데, 무시하거나 지워도 돼요.
- **미리보기 GIF와 MP4는 v0.3.2 기준이에요.** v0.4.0 출격 패턴은 아직 안 들어가 있어요.

---

## 3. 빌드랑 테스트

### 게임 안에서

1. .NET 8 SDK가 필요해요. x64 설치본이어야 하고, 9나 10은 안 돼요.
2. tModLoader에서 워크샵에 들어가 모드 소스들을 열고, **빌드 + 리로드**를 눌러요.
3. `en-US` 번역 파일을 tModLoader가 자기 형식으로 다시 쓰면 "Rebuild Required"가 떠요. 다시 빌드하면 돼요. 번역을 고칠 때는 **기기에 있는 파일을 기준으로** 고쳐요.
4. `build.txt`가 한동안 빈 파일이었어요 (9월 19일부터). 그동안은 기본값으로 빌드됐을 거예요. v0.4.0에서 `displayName = Modern Arsenal`로 다시 채웠고, 지금 버전은 `0.4.1`이에요. 제작자 칸은 비워뒀으니 필요하면 사용자한테 물어보고 넣어요.

### 테스트 명령어 (싱글 전용)

| 명령어 | 동작 |
|---|---|
| `/apache` | 아파치 소환. 골렘 조건을 무시하고, 무전 소리와 진입 메시지가 나옴 |
| `/apache radio [n]` | 무전기 n개 지급 (최대 20) |
| `/apache hp [1-100]` | 떠 있는 아파치의 체력을 %로 설정. 페이즈 확인용 |
| `/apache kill` | 드랍 없이 제거 |
| `/apache m4` | M4A1 카빈과 무한 머스킷 탄 주머니 지급 (`/apache m4a1`도 됨) |
| `/m4a1` | 위와 같음. 따로 만든 짧은 명령어 |

`/apache hp 65`, `35`, `10` 순서로 치면 페이즈마다 출격 연출을 차례대로 볼 수 있어요. 출격 중에는 전환이 안 끼어드니까, 한 번 끝나고 재배치로 돌아온 다음에 쳐요.

### 게임 없이 컴파일 검사 (다음 에이전트용)

이게 제일 중요해요. 이거 없이 코드 고치면 추측으로 짜게 돼요.

1. 클라우드에 SDK를 깔아요: `apt-get update && apt-get install -y dotnet-sdk-8.0`
2. 사용자 PC의 tModLoader 설치 폴더에서 참조 dll을 가져와요 (device_stage_files).
   - `tModLoader.dll`, `tModLoader.xml`
   - `Libraries/` 아래: `FNA`, `ReLogic`, `TerrariaHooks`, `log4net`, `newtonsoft.json`, `steamworks.net`, `monomod.*`, `mono.cecil`, `hjson`, `jetbrains.annotations`, `NVorbis`, `MP3Sharp`, `CsvHelper`, `RailSDK.Net`, `SteelSeriesEngineWrapper`, `dotnetzip`, `humanizer.core`, `stubble.core`, `utf.unknown`
3. `ModernArsenal_tools.zip` 안 `tools/compile_check/`에 있는 `MACheck.csproj`랑 `nuget.config`를 써요. (zip이 없으면 새로 만들면 돼요: net8.0 Library, `DefineConstants`에 `TML_2026_07;FNA`, `Compile Include="../src/**/*.cs"`, 참조는 `tModLoader.dll`과 `Libraries/**/*.dll`. v0.4.4 때 이렇게 만들어서 썼어요.) nuget.config는 소스를 비워둔 거예요. 프록시가 nuget.org를 막아서, 이게 없으면 복원 단계에서 멈춰요.
4. csproj 안의 경로를 맞추고 `dotnet build --no-incremental`을 돌려요. 에러 0개를 확인한 다음에 사용자 폴더에 반영해요.

---

## 4. 프로젝트 구조

```
ModernArsenal/
├─ build.txt / description.txt / icon.png / ModernArsenal.csproj
├─ ModernArsenal.cs                    Mod 클래스, 네트워크 패킷(SummonApache)
├─ Assets/
│  ├─ Music/ApacheTheme.ogg            보스 테마 (루프 태그 포함)
│  ├─ Sounds/*.ogg                     효과음 25개 (M4 사격 4, 조정간 1, 탄피 4, 무전 단말 2 포함)
│  └─ Textures/UI/FieldRadio_Apache.png  단말기용 아파치 실루엣 (184×60, 2x 도트)
├─ Common/
│  ├─ Balance.cs                       적 투사체 피해 보정 (Hostile(x) = x/2)
│  ├─ ModSounds.cs                     SoundStyle 전부 여기
│  ├─ Commands/ApacheCommand.cs        /apache 테스트 명령어
│  ├─ Commands/M4A1Command.cs          /m4a1 테스트 명령어
│  ├─ UI/FieldRadioUI.cs               화력 지원 단말기 (무전기를 쓰면 열림)
│  └─ Systems/
│     ├─ CalamityCompat.cs             칼라미티 난이도 감지 (Call 기반, 하드 참조 없음)
│     ├─ DownedBossSystem.cs           downedApache 월드 플래그
│     └─ ApacheWarningSystem.cs        화면 상단 WARNING 배너 (UI 레이어)
│  └─ Bosses/                          보스 공용 (v0.4.4)
│     ├─ BossParts.cs                  IPartOwner, BossPart(부위 NPC 기반), PartSet(스폰, 가림 판정, 동기화)
│     └─ BossFx.cs                     바닐라 연기 고어 61~63 (틱당 36개 상한)
└─ Content/
   ├─ Buffs/MiniApacheBuff             미니 아파치 버프
   ├─ Dusts/                           SmokePuffDust, FlareSmokeDust
   ├─ Gores/ApacheGore_*.png           추락 파편 8종 (경로에 "Gores/"가 있어야 자동 로드됨)
   ├─ Gores/M4A1Casing.cs / .png        M4A1 탄피 (ModGore, 이름이 같은 png랑 자동으로 묶임)
   ├─ Items/ApacheSummon               소환 무전기
   ├─ Items/Weapons/                   MiniApacheRemote, AirstrikeDesignator
   │  └─ M4A1.cs                       M4A1 카빈과 M4A1Player (사격 애니메이션 타이머), 텍스처 M4A1 / _Held / _Flash
   ├─ NPCs/Shared/DecoyFlare.cs       적 판정 교란 플레어 (모든 보스 공용, v0.4.4)
   ├─ NPCs/Apache/
   │  ├─ ApacheBoss.cs                 스탯, 동기화, 파츠 관리, 소환, 사망 처리
   │  ├─ ApacheBoss.AI.cs              페이즈 전환, 셔플 백, 비행 모델, 무전과 시스템 로그
   │  ├─ ApacheBoss.Attacks.cs         일반 패턴 전부, 무장 발사, 추락
   │  ├─ ApacheBoss.Sortie.cs          출격 (페이즈마다 상공 이탈, 위에서 타격, 재진입), v0.4.0 신규
   │  ├─ ApacheBoss.Visuals.cs         로터 소리, 손상 연출, 그리기
   │  ├─ ApacheParts.cs                파츠 NPC 4종 (기관포, 포드, 레이더, 꼬리 로터)
   │  └─ *.png                         본체와 오버레이 스프라이트 (03번 파일 참고)
   └─ Projectiles/
      ├─ Apache/                       적 탄: 30mm, 히드라, 헬파이어(유도/상부공격), 플레어, 텔레그래프
      ├─ Explosions/ExplosionBase.cs   폭발 그리드 시트 (적군용/아군용)
      └─ Summon/                       미니 아파치, 공습 (마커, 곡사 로켓, 건쉽 기총소사)
```

C# 코드는 약 4,980줄이에요.
