# ModernArsenal 인수인계 03: 도트와 스프라이트 (v0.5.12)

> 여러 파일로 나눠 둔 인수인계서 중 하나예요. 전체 목록은 `00_먼저_읽기.md`에 있어요.

## 1. 아파치 스프라이트

### 1-1. 도트 규칙

이건 사용자가 제일 민감하게 보는 부분이에요. 절대 어기지 마요.

- **1x로 찍고 2배 최근접 확대해요.** 모든 도트는 2×2 블록이에요. 게임에서 크기를 바꿔 그리면 격자가 깨지니까, 크기가 다른 버전이 필요하면 **따로 샘플링해서 새 파일로** 만들어요.
- 이유: 한 번 불꽃을 2배로 키워 그렸다가 도트가 4×4로 뭉개져서 사용자한테 크게 욕먹었어요.
- 텍스처 한 장은 2048px를 넘으면 안 돼요. 긴 애니메이션은 격자 시트로 만들어요.
- 본체는 **왼쪽을 보는 게 기준**이에요. 오른쪽을 볼 때는 좌우 반전하고 오프셋 X를 뒤집어요.

### 1-2. 본체 좌표 (텍스처 픽셀, 본체 중심 (186,63) 기준)

| 부위 | 텍스처 좌표 | 오프셋 |
|---|---|---|
| 메인 로터 축 | (141,39) | (-45,-24) |
| 꼬리 로터 허브 | (337,59) | (151,-4) |
| 기관포 피벗 | (81,99) | (-105,36) |
| 로켓 포드 앞끝 | (115,93) | (-71,30) |
| 로켓 포드 중심 | (137,92) | (-49,29) |
| 레이더 돔 중심 | (141,24) | (-45,-39) |
| 레이더 받침 | (143,33) | (-43,-30) |
| 엔진 배기구 | (198,66) | (12,3) |
| 불꽃 뿌리 (스프라이트 왼쪽 아래) | (190,70) | (4,7) |

- 기관포 스프라이트는 42×14이고 피벗은 (33,7), 총열 길이는 33이에요. 조준 범위는 동체선 기준 위로 15°, 아래로 70°예요.
- 메인 로터 시트는 프레임 높이 18, 간격 20, 6프레임이에요. 틱당 29° 회전하고, 꼬리 로터는 그 4.86배로 돌아요.
- 레이더 돔은 본체에서 잘라내서 따로 그려요 (`_Radar`). 부서지면 `_RadarStub`으로 바꿔 그려요.

### 1-3. 오버레이 (전부 372×126, 본체와 같은 원점과 회전으로 그림)

`_Damage1`, `_Damage2`, `_Crack1`, `_Crack2`, `_PodWreck`, `_EngineGlow`

### 1-4. 미니 아파치

보스 스프라이트 전부(동체+레이더+기관포, 로터 시트, 꼬리 로터, 블러)를 **정확히 절반**으로 샘플링했어요. 동체는 186×64, 로터 프레임은 140×10(간격 12)이에요. 오프셋은 보스 값의 절반이고, 보스랑 같은 방식으로 그려요.

---

## 2. 도트 작업 방식

사용자가 좋아한 방식은 **참고 그림을 트레이스하는 방식**이에요. 아파치도 이렇게 해서 통과했어요.

1. 참고 그림을 재질별로 나눠요 (선, 외투, 망토, 금장, 피부 같은 식).
2. 먹선은 모폴로지 열기로 얇은 선만 골라내서 가까운 재질에 넘겨요. 두꺼운 검은 면만 남겨요.
3. 목표 칸 수로 줄일 때 각 칸은 재질의 가중 다수결로 정해요.
4. 명암은 참고 그림의 밝기를 재질 안에서 분위수로 나눠서 가져와요.
5. 미리 정해둔 색 램프에 대응시키고 바깥 윤곽선을 둘러요.

맨손으로 찍은 초안은 두 번 연속으로 크게 욕먹었어요. 얼굴을 "고친다"고 손댔다가 더 나빠진 적도 있어요. 사용자가 좋다고 한 결과는 건드리지 마요.

다만 **총처럼 딱딱한 기계는 예외**예요. M4A1은 사진 트레이스가 뭉개져서, 사진을 격자로 재서 부품 위치를 잡은 다음 칸 단위로 직접 찍었어요 (3장).

---

## 3. M4A1 스프라이트

| 파일 | 크기 | 내용 |
|---|---|---|
| `M4A1.png` | 84×32 (42×16칸) | 인벤토리와 월드용. 오른쪽을 봄 |
| `M4A1_Held.png` | 84×128 | 들고 쏠 때 쓰는 4프레임 세로 시트. 배출구 안쪽만 다름 |
| `M4A1_Flash.png` | 20×42 | 총구 화염 3프레임 (큰 불꽃, 작은 불꽃, 잔불), 각 20×14 |
| `Gores/M4A1Casing.png` | 6×4 (3×2칸) | 5.56 탄피. 왼쪽이 림 |

- 참고 사진은 M4의 **왼쪽 면**이에요. 배출구는 오른쪽 면에만 있어서, 사진을 뒤집어 쓰면 배출구가 안 보여요. 그래서 오른쪽 면 기준으로 배출구, 열린 먼지 덮개, 노리쇠를 직접 그려 넣었어요. 사용자가 "왜 배출구가 안 보이냐"고 지적한 부분이에요.
- 배출구는 칸 좌표 x 15~17, y 4~5 안쪽이에요. 프레임 0은 노리쇠가 닫힌 상태, 1은 탄피가 빠지는 중(황동 픽셀), 2는 노리쇠가 뒤로 빠진 상태, 3은 돌아오는 중이에요.
- 색은 금속 램프 6단(푸른 회색)과 폴리머 램프 5단(무채색)을 나눠 썼어요. 개머리, 손잡이, 핸드가드가 폴리머 쪽이에요. 윤곽선은 (20,20,26)이에요.
- 좌표 기준점 (텍스처 픽셀): 총구 (84, 9), 탄피 나오는 곳 (33, 8), 손잡이 윗부분 약 (22, 19).
- 생성 스크립트는 `ModernArsenal_tools.zip`의 `tools/sprites/m4a1/m4final.py`예요. 행마다 구간 문자열로 적어서 열이 밀리지 않게 했어요. `grid42.py`는 사진 위에 42칸 격자를 그려서 부품 위치를 재는 용도예요.

---

## 4. NPC 도트 (보류 중)

사용자가 준 캐릭터 그림으로 마을 NPC를 만들던 중이에요. 사용자가 "일단 냅두고 총기나 만들어볼래"라고 해서 멈췄어요.

- 조건: 마을 NPC, 고퀄리티, 창은 공격할 때만 꺼냄.
- 바닐라 마을 NPC 규격은 40×56 (20×28칸), 25프레임이에요. 0 서기, 1 점프, 2~15 걷기, 16~17 팔 들기, 18 앉기, 19 대화, 20 눈 깜빡임, 21~24 공격이에요. `ExtraFramesCount = 9`, `AttackFrameCount = 4`로 두고, `AttackType`은 0 던지기, 1 사격, 2 마법, 3 근접 중에서 골라요.
- 사용자가 좋다고 한 결과: `trace_40_liked.png` ("이건 잘했잖아"), 그리고 여기서 잡티 두 개만 지운 `npc_idle_v1.png`.
- 사용자 제안: "슈칼 NPC처럼 통통하게 하면 24칸도 괜찮을 듯". 그래서 통통한 24칸 두 안을 보냈고 **아직 고르지 않았어요.**
  - A안 `trace_r1.png`: 세로 구간 `[[15,290,9],[290,600,9],[600,919,6]]`, 가로 구간 `[[90,230,3],[230,530,12],[530,680,3]]`
  - B안 `trace_r2.png` (추천): 세로 구간 `[[15,290,10],[290,600,8],[600,919,6]]`, 가로 구간 `[[90,230,3],[230,530,13],[530,660,3]]`
- 실행: `python3 trace2.py 24 3 '<json 구간>' <태그> recolor`. `recolor`를 붙이면 안쪽 검정을 망토의 짙은 빨강과 외투 갈색으로 바꿔요. 참고 그림은 `ref_crop.png` (원본 (150,40)~(900,970) 잘라낸 것, 창은 다각형으로 지움), 재질 맵은 `cls.npy`예요.
- 크기 설명 실수가 한 번 있었어요. "조금 크게 28×40칸"이라고 했던 안은 사실 바닐라의 두 배쯤이라 비교 그림으로 다시 보여줬어요.
- 다음 단계: 사용자가 A나 B를 고르면 25프레임 시트를 만들고, 공격 프레임에만 창을 붙이고, NPC 코드(대화, 상점 여부, 이름 목록, 공격)를 짜요. 도트 관련 파일은 전부 `tools/sprites/npc/`에 있어요.

---

## 5. 험비 스프라이트 (보스 1번, 0.5.0부터 게임에 들어감)

- 위치: 텍스처는 `ModSources\ModernArsenal\Content\NPCs\Humvee\`, 떨어져 나가는 파츠 고어는 `Content\Gores\Humvee\`. 생성 스크립트, 미리보기, 좌표 JSON은 `humvee_sprites\`.
- 기준 그림: 첫 번째 측면 렌더(`ref_side.png`)로 차체를 쟀고, 사용자가 나중에 준 M1151 측면 사진(`ref_m1151.png`) 기준으로 앞 오버행과 보닛(거의 평평하다가 끝에서만 떨어짐), 포탑을 고쳤어요.
- 포탑은 M1151 O-GPK 모양이에요. 뒷모서리가 기울었고, 큰 방탄창 두 개가 있고, 앞쪽은 사선으로 떨어진 뒤 낮은 턱이 있어요. M2는 포탑 위로 드러나 있고, 앞 방패는 옆에서 보면 얇은 판 하나예요. 사용자가 "포탑 꼬라지" 지적을 두 번 했으니 이 모양을 유지해요.
- 사용자 지적으로 뺀 것: 뒤쪽 짐칸 옆의 삼각형 루버. 문은 안으로 파인 게 아니라 튀어나와 보여야 해요 (윗단과 빛 받는 모서리는 밝게, 반대쪽과 아래는 어둡게, 발판과 카울에 그림자).
- 입체감은 지붕, 짐칸 윗면, 보닛 윗면을 2칸 정도 보이게 해서 냈어요 (살짝 위에서 보는 시점).
- 캔버스 224×120px (112×60칸), 왼쪽을 봐요. 모든 레이어가 같은 원점이라 같은 위치에 겹쳐 그리면 돼요.
- 그리는 순서: Frame, Engine, Cabin, Interior, WellRear, WellFront, Spare, Wheel(뒤), Wheel(앞), Body, DoorRear, DoorFront, Hood, Antenna, TurretShield, Gun, Turret.
- 바퀴 중심 (텍스처 px): 뒤 (177,95), 앞 (41,95). 바퀴 시트는 42×42 프레임 4장, 세로, 간격 2px. 한 프레임이 11.25° 회전이라 4장이 한 바퀴의 1/8이에요 (볼트 8개라 이음매 없이 반복).
- M2 (`_Gun`) 72×14px, 피벗 (49,13), 캔버스 안 피벗 (93.8,22.4). TurretShield 다음, Turret 전에 그려요. 그래야 측면 장갑판이 기관총 몸통 뒷부분을 가리고 총열은 방패 앞으로 지나가요.
- 문 유리는 알파 230~235라 뒤의 실내가 살짝 비쳐요. Cabin 레이어의 반대편 창은 뚫려 있어서 문을 떼면 차 너머 배경이 보여요.
- Body 레이어는 문 자리에 1칸짜리 문틀만 남기고 뚫려 있어요. Hood 레이어는 앞 펜더까지 통째로예요 (실제 험비처럼 앞부분이 한 덩어리로 들림).
- 스크립트: `pix.py`(칸 단위 도구), `ext.py`(외장), `inner.py`(프레임, 파워트레인, 실내), `sprites.py`(엔진, 좌석, 핸들, 무전기 도트 맵), `assemble.py`(합성), `export.py`(내보내기). `python3 export.py`로 전부 다시 뽑혀요.


---

## 6. AH-64E 가디언 스프라이트 (0.5.7부터 게임에 들어감)

- 게임에는 `Content\NPCs\Apache\ApacheGuardian*.png`로 들어가 있어요. 원본 작업 폴더는 `apache_e_sprites\`이고 거기서는 파일 이름이 `ApacheEBoss*.png`예요. D 텍스처와 크기·원점·파츠 배치가 같아요. 몸통 372×126 (186×63칸), 중심 (186,63).
- 들어 있는 것: 몸통, `_Radar`, `_MainRotor`, `_MainRotorBlur`(D와 같음), `_TailRotor`, `_TailRotorBlur`, `_Gun`(D와 같음), `_Head_Boss`, `SpikeNLOS.png`(날개 편 것, 30×12, 기수가 오른쪽), `SpikeNLOS_folded.png`, 비교 그림 `preview_D_vs_E.png`, 단독 그림 `AH-64E_Guardian.png`, GIF `apache_e_spike.gif`.
- 다시 뽑는 법: `build.py`를 클라우드에서 실행해요. D 원본 경로는 클라우드 기준(`/home/claude/mod/src/...`, `/mnt/user-data/uploads/...`)이라, 새 세션에서는 기기 `Content\NPCs\Apache\`의 D 텍스처를 스테이징하고 경로만 고쳐요. 1x에서 작업하고 2x 최근접으로 내보내요.
- 사용자 방침: **엔진 나셀과 동체는 D 도트 그대로**(색만 바꿈), **배기구 커버만 E식**. 나셀을 새로 그리면 안 돼요.
- 도장: 저시인성 회색 램프 `GREY` 10단계(올리브 램프를 1:1로 대응, 약간 따뜻한 중립 회색). 레이더 돔은 한 단계 어둡게. 금속부는 한 단계 어둡게(`METAL`).
- E에서 바뀐 부분 (1x 칸 좌표, 왼쪽을 봄):
  - 블랙홀 배기 커버: (93,30)부터 9×10 스탬프. 비스듬한 끝면에 루버 달린 검은 출구. 뒤쪽 D 루버 잔해는 지움.
  - 흰 V 마크 자리에 저시인성 라운델 (79~83, 31~35).
  - "U.S. ARMY" 4줄 글씨 (89,41)부터, 배경만 살짝 정리하고 3톤 어둡게.
  - 배기 그을음: 페어링과 붐 윗선을 따라가는 두 줄.
  - 히드라 포드를 떼고 **스파이크 NLOS 런처**: 모래색 캐니스터 2단, x 50~75, 행 46~54. 위 레일 (55~73, 44~45). 런처 앞머리 위에 기울어진 데이터링크/레이더 포드(런처 앞선과 같은 위치, x 50~58).
  - 윙팁 ASE/MAW 포드: (66,38)부터, 위는 둥글고 아래는 V자. **사용자가 모양이 틀렸다고 했음, 다시 그릴 것.**
  - 꼬리 로터 기어박스 불룩한 부분: 허브 (168.5,29.5) 둘레를 반지름 4.3으로 음영 처리.
  - 기수 미사일 경보 센서, 배기 뒤 재머 볼, 배 밑 블레이드 안테나 2개, 붐 밑 ASE 박스 2개, UTA 안테나.
  - 로터: 블레이드 끝이 한 칸 아래로 꺾임(E 복합재 블레이드의 후퇴·하반각 팁). 측면 프레임(3번)은 건드리지 않음.
- 코드에서 쓸 좌표 (2x px, 몸통 중심 기준, 왼쪽을 볼 때): 날개 항법등 (-15, 20), 스파이크 발사구(위 캐니스터 입구) (-88, 34), 런처 중심 (-61, 38). 나머지(로터 축, 꼬리 허브, 기관포 피벗, 레이더, TADS, 비컨, 꼬리등)는 D와 같아요. D의 `RocketPod`/`PodCenter`는 E에서 쓰면 안 돼요(포드가 없어짐).
- 아직 안 한 것: 피격 오버레이(_Damage1/2, _Crack1/2, _PodWreck, _EngineGlow, _EngineFire)는 기기에만 있고 E용으로 안 바꿨어요. 올리브가 섞여 있으면 회색으로 바꾸고, `_PodWreck`은 스파이크 런처 모양으로 다시 그려야 해요.
- GIF 렌더러: 클라우드 `/home/claude/gif/render_apache_e.py` (사본이 이 폴더에 있음). `render_apache.py`를 바탕으로 E 스프라이트와 스파이크 발사 구간을 붙인 것이라 `gifhud.py`와 치누크 `_Glow.png`가 필요해요.

---

## 7. MQ-1C 그레이 이글 스프라이트 (가디언의 UAV, 0.5.7부터 게임에 들어감)

- 사용자가 UAV를 고정익 **MQ-1C 그레이 이글**로 정했어요. 게임에는 `Content\NPCs\Guardian\GrayEagleUAV*.png`로 들어가 있어요 (소환수 `GrayEagleMinion`도 같은 텍스처). 원본 작업 폴더는 `gray_eagle_sprites\`예요.
- 사용자가 준 측면 사진(`ref_side.png`, 기수가 오른쪽)을 부위별 다각형으로 트레이스하고, 칸마다 사진 밝기로 음영을 넣은 뒤 왼쪽을 보게 뒤집었어요. 1칸 = 사진 5px. `build_uav.py`로 다시 뽑혀요(클라우드 경로 기준).
- `GrayEagle.png` 202×58 (101×29칸). 사진처럼 먼 쪽 날개와 꼬리가 위로 살짝 보이고, 가까운 날개는 아래로 보여요. 파일런, 고정식 기어, 기수 센서 볼, 피토관, "ARMY" 검은 글씨.
- 헬파이어: 사용자 지시로 모드의 `ApacheHellfire.png`(검은 L 헬파이어)를 그대로 가져와 기수가 왼쪽을 보게 돌리고(전치), 흰 레이돔만 IR/레이저 시커 유리로 바꿨어요. 1x 28×9칸, 캔버스 (38,17)에 붙음. 게임에서 그레이 이글이 쏘는 헬파이어는 0.5.12 이후 이름을 **AGM-114R**로 따로 달았어요 (레이저 유도라서. 텍스처는 아파치 것과 같음).
- 등화 (사용자 지정, 코드는 `Common/Visuals/NavLightRig.cs`, 좌표는 `GrayEagle_lights.json`, 2x px, 텍스처 중심 기준, 왼쪽을 볼 때):
  - 스프라이트 렌즈는 전부 무채색이에요. 좌우 반전하면 색이 뒤집히니까 색은 코드가 방향 보고 칠해요.
  - 왼쪽을 볼 때(`spriteDirection == -1`, 좌현): 보이는 날개끝 적색, 위로 보이는 먼 날개끝 녹색(흐리게). 오른쪽을 볼 때는 반대.
  - 공통: 편대 비행등(슬라임 라이트, 옅은 형광 연두) 마스크 `GrayEagle_Slime.png`(기수 옆, 꼬리날개, 동체 뒤), 백색 스트로브 60틱마다 3틱 번쩍(날개끝 둘, 꼬리), 위/배 적색 비콘은 72틱 주기로 반씩 어긋나게 페이드, 꼬리날개 뒤 백색 미등 상시.
  - 낮에는 글로우를 줄이고 작은 색 점을 그려서 하얗게 뜨지 않게 했어요(아파치 항법등 문제의 해결 방식, 아파치에는 아직 안 붙임).
  - "ARMY" 글씨는 몸통에서 빼서 `GrayEagle_MarksL.png`/`MarksR.png`로 분리했어요. 반전 없이 방향에 맞는 쪽을 그려요(`DrawDecals`).
  - 쓰는 법: `NavLightRig.GrayEagle(Texture)`로 만들고, PreDraw에서 몸통 그린 뒤 `DrawDecals`, PostDraw에서 `Draw`. `GrayEagleUAV.cs`와 `GrayEagleMinion.cs`가 이렇게 써요.
  - 편대등 마스크 `_Slime`은 0.5.12에서 LED 칸 띠로 바꿨어요. 켜진 칸 (204,228,182)/(176,198,160)을 번갈아 쓰고 2~3칸마다 구분 칸 하나를 마스크에서 뺐어요 (`v0512_tools/led_strips.py`).
- 프롭: `GrayEagle_Prop.png` 3날 옆모습 6프레임(프레임 높이 27칸, 간격 2칸), `GrayEagle_PropBlur.png`. 축은 (93,10)칸.
- 미리보기 `preview.png`, 프롭과 항법등 애니메이션 `idle.gif`.

---

## 8. RQ-7 섀도 스프라이트 (0.5.8부터 1:1 픽셀판)

- **지금 게임에 들어간 건 1:1 픽셀판이에요.** `Content\NPCs\Guardian\ShadowUAV*.png`, 몸통 76×34. 아파치의 약 1/5 크기로 줄이면서 디테일을 살리려고 2배율이 아니라 1:1로 그렸어요 (이 모드에서 2x 규칙의 유일한 예외, 사용자와 합의).
- 원본과 스크립트: `rq7_shadow_sprites_fine\` (`build_shadow_fine.py`, SX 24.5, SY 14.5). `ShadowUAV.png`, `_Prop`(프레임 높이 13, 간격 2), `_PropBlur`, `_Slime`, `ShadowUAV_lights.json`.
- 트레이스 기준은 사용자가 준 MVR 측면 렌더(기수가 이미 왼쪽)예요. **사용자 지적: 너무 얄쌍하면 안 됨.** 그래서 가로보다 세로를 촘촘하게 샘플링해서 일부러 통통하게 했어요. 역V 꼬리, 뒤쪽 엔진, 푸셔 2날 프롭, 기수 스트럿과 바퀴, 센서 볼.
- 코드 좌표 (`ShadowUAV.cs`): 판정 70×26, 센서 볼 (-18.5, 5.5), 프롭 축 (2.5, -2.5). 등화는 `NavLightRig.RQ7Shadow(...)`, 오프셋은 1:1 스프라이트 기준, 렌즈 크기 0.6.
- 편대등 마스크 `_Slime`은 0.5.12에서 LED 칸 띠로 바꿨어요 (그레이 이글과 같음, 7장).
- 예전 큰 버전(`rq7_shadow_sprites\`, 218×80, 2x)은 이제 안 써요. 참고용으로만 남겨 둠.

---

## 9. 등장 GIF v2~v4와 스파이크 전개 시트 (`apache_e_sprites/entrance_v2_v4/`)

- 등장 GIF v2 (`entrance_v2_v4/apache_e_entrance_v2.gif`, 생성 스크립트 `render_apache_e_v2.py`): D와 다른 구도. 카메라는 플레이어 쪽에 낮게 두고 오른쪽을 비워 둠. E가 오른쪽 화면 밖에서 저공으로 기수를 숙이고 돌입(먼지 꼬리), 급제동하며 기수를 들고 플레어, FCR 조사로 `위치 발각됨`, 기관포 슬루와 TADS 레이저, 타이틀은 왼쪽 아래. 이어서 스파이크 NLOS를 높게 로프트시켜 내리꽂고, 카메라가 미사일을 따라간 뒤 넓은 구도로 빠짐. 스크립트는 같은 폴더의 스프라이트와 `CutsceneStripes.png`, `HumveeChinook_Glow.png`만 있으면 돌아감(`gifhud.py` 불필요). 예전 `apache_e_spike.gif`는 D와 같은 구도라 참고용으로만 남겨 둠.
- 등장 GIF v4 (`entrance_v2_v4/apache_e_entrance_v4.gif`, 스크립트 `render_apache_e_v4.py`, v5 이전 안): 오른쪽 멀리서 천천히 활공해 들어와 약 1500px 밖에서 멈춤. 가디언 전용 경보는 청록 데이터링크 스타일에 `TARGETED` / `표적 지정됨`(모서리 깎인 판, 안쪽으로 모이는 쉐브론, 다이아몬드 락 아이콘). 원거리에서 스파이크 NLOS 발사, 발사 순간은 2배 클로즈업 슬로모션으로 날개 전개를 보여 줌. 로프트 구간은 미사일 추적 카메라, 종말 강하는 스파이크 시커 화면(EO, 흑백, 단계 줌, RNG와 TTI 표시), 명중하면 화면이 하얗게 날아간 뒤 측면 폭발. 그다음 가디언으로 휙 돌아오면 MQ-1C(로터 위)와 RQ-7B(스키드 아래)가 오른쪽에서 왼쪽으로 스쳐 지나가고 타이틀(`롱보우 · 스파이크 NLOS · MUM-T`). 무인기는 아직 임시 사각형. v3는 날개 전개가 없는 같은 구성.
- 스파이크 새 도트 `SpikeNLOS_Deploy.png` (생성 `spike_sprite.py`): 사용자 참고 사진 기준. 밝은 회색 몸통, 검은 시커 돔, 파란 표기 흔적, X자 주날개(힌지는 꼬리에서 약 40%), 작은 꼬리핀 4개. 세로 시트 5프레임(40x34px, 간격 2px, 기수 오른쪽): 0 접힘(날개가 몸통을 따라 뒤로 누움), 1~3 전개 중, 4 전개 완료. 게임에서는 발사 후 3틱까지 0번, 이후 2틱마다 한 프레임. 회전 원점 (20,16), 모터 불꽃은 원점에서 19px 뒤. 예전 `SpikeNLOS.png`/`_folded.png`는 남겨 둠.
- 이 폴더의 `SpikeNLOS_Deploy.png`가 v4 원본 시트예요. `entrance_gif_v5/`에 있는 건 스크립트로 다시 뽑은 것이라 픽셀이 달라요(원본 픽셀 md5 7bb07dfb…, 재생성본 27da80d5…).
- 스크립트는 같은 폴더의 파일과 `apache_e_sprites/`의 E 스프라이트를 읽어요. 돌릴 때는 스프라이트를 이 폴더에 같이 두거나 `assets/`로 모아요.

- **등장 GIF v5 최종본 (`entrance_gif_v5_final/`, 현재 기준안)**: 사용자 v4를 바탕으로 이 세션에서 다시 만든 것. `entrance_gif_v5/`(다른 세션판)는 대체됨.
  - v4에서 바뀐 것: 오프닝을 플레이어에서 가디언까지 천천히 패닝(96틱)해서 찾는 방식으로 바꿈. 경보는 v4의 청록 데이터링크 스타일(`TARGETED` / `표적 지정됨`) 유지. 발사할 때 캐니스터 뒤로 후폭풍 구름, 미사일 연기는 퍼프마다 수명이 따로라 발사관 쪽부터 옅어짐(SmokeTrails 방식). 스파이크 전개 시트는 v4 원본(픽셀 md5 7bb07dfb…). 시커 화면은 거리 700부터 시작하고, 시커 화면 안에 미사일 자신과 자기 연기가 찍히던 문제를 고침. 명중하면 흙덩이가 튀었다 떨어지고, 연기 기둥이 서고, 땅에 그을음이 남음. 무인기는 실제 그레이 이글(로터 위)과 섀도(스키드 아래) 스프라이트에 프롭, 항법등, 날개끝 와류를 넣었고, 이글이 지나갈 때 카메라가 따라 기울어짐. 타이틀 부제는 게임 번역문 `롱보우 · 스파이크 NLOS · 유무인 복합`.
  - `render_apache_e_v5.py`는 옆의 `assets/`만 있으면 돌아감(E 스프라이트, 스파이크 시트, 드론 스프라이트와 등화 json, 줄무늬, 글로우). `python3 render_apache_e_v5.py [출력 폴더]`. 210프레임, 1000x720.
  - 게임 컷신은 0.5.10에서 청록 `WarningStyle.Guardian`과 `TARGETED`를 넣었고, 0.5.11에서 이 v5 흐름을 게임에 옮겼어요. 0.5.12부터는 전투 중 경보와 조준 표시도 청록이에요.


---

## M230과 Mk19 (v0.5.12)

- 사용자가 준 사진 두 장(`v0512_tools/ref/m230.png`, `mk19.png`)으로 만들었어요. 맨손으로 찍은 두 판은 "개구리"라고 욕먹었고, 이 방식이 통과했어요.
- 방법: 사진 위에 격자를 그려 부품 위치를 칸 단위로 재요(M230은 사진 7px가 1칸, Mk19는 사진을 13도 돌려서 총열을 수평으로 맞춘 뒤 8px가 1칸). 부품 모양은 그 좌표로 직접 놓고, 부품 안쪽 음영은 칸마다 사진 밝기를 주변 칸 중앙값으로 다듬어서 부품별 색 램프에 대응시켜요. 윗면은 한 단계 밝게, 아랫면은 한 단계 어둡게 한 다음 외곽선을 둘러요.
- 부품끼리 만나는 자리에는 한 칸짜리 어두운 이음선을 넣어요(`seams()`). 사용자가 "부품 내부 선만 구별되게, 나머지는 건드리지 말 것"이라고 한 뒤 넣은 거라, 음영을 다시 손대지 마요.
- 스크립트: `v0512_tools/m230_v6.py`, `mk19_v6.py`. 결과: `M230ChainGun.png` 136x28, `Mk19Launcher.png` 120x48.
