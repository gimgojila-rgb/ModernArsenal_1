# ModernArsenal 인수인계 08: 변경 이력 (v0.5.12)

> 여러 파일로 나눠 둔 인수인계서 중 하나예요. 전체 목록은 `00_먼저_읽기.md`에 있어요.

## 변경 이력

| 버전 | 내용 |
|---|---|
| 0.1.0 | 아파치 보스 첫 버전, 사운드 7종 |
| 0.2.0 | 4페이즈, 텔레그래프와 경보, 파츠 파괴, 손상 연출, 추락, OST, 플레어 무한 발사 버그 수정 |
| 0.3.0 | 드랍템 2종, 긴 돌진과 짧은 돌진 로켓, 적재량 기준 탄막, 파츠 불량 모드, 셔플 백, 헬파이어 유도 하향, 진입 메시지, 무전/시스템 색 분리, 로터 소리 잔존 버그 수정, 음악 우선순위 상향 |
| 0.3.1 | 장외 사격 패턴, 헬파이어 유도 15% 상향, 엔진 불 재작업, 미니 아파치를 절반 샘플링으로 교체, 아이콘 재작업, 공습 로켓 15발로 정정 |
| 0.3.2 | 긴 돌진을 데오갓식으로 재작업, 등장 빈도 상향, 무전과 시스템 로그를 영어 실무전 말투로 교체 (한국어판 포함), 곡사 헬파이어가 상승 중에 과통과 판정을 받아 유도를 안 하던 버그 수정 |
| 0.4.0 | 아파치 리메이크: 매 페이즈 시작에 출격 (상공 이탈, 위에서 락온 헬파이어와 히드라, 재진입 강하. 3페이즈부터 관통, 4페이즈 2회). 상공에 있는 동안 무적. 헬파이어 유도 20% 상향 (락온 포함). 빈 build.txt 복구 |
| 0.4.1 | M4A1 카빈 추가 (스켈레트론 이후 제작, 우클릭으로 단발과 자동 전환). 사격 애니메이션: 배출구 안 노리쇠 4프레임, 반동과 총구 들림, 총구 화염 3프레임, 튀고 구르는 탄피 고어와 금속음 |
| 0.4.3 | (다른 세션) 무전기는 다시 바로 소환, 레이저 목표 지시기 우클릭으로 근접 항공 지원 단말기와 아군 AH-64D |
| 0.4.4 | 플레어를 적 판정 교란 NPC로 교체(유도, 소환수, 비유도 탄을 끌어감. 한 방에 터지고 드랍, 회복, 피해 숫자 없음, 최대 24개). 3페이즈부터 스탠드오프 진입 때 플레어 커튼. 폭발과 파츠 파괴에 바닐라 연기 고어 61~63. 부위 시스템 공통화(IPartOwner, BossPart, PartSet). 험비와 M2 소리를 ModSounds에 등록 |
| 0.5.0 | 험비 보스(M1151, 눈 이전, 데저트 스커지급 4000 HP). 점프 없음: 발판 주행, 기울어져 올라오기, 낮은 벽 넘기, 못 가면 측면 우회. M2 점사, Mk19 곡사(발판에서도 폭발), 돌진, 2페이즈 문짝 날리고 TOW. 부위: 포탑, 앞뒤 타이어, 엔진(2페이즈). 도로 신호탄 소환, `/humvee` 명령어, 전용 브금 Armored Perimeter, BossChatter 공통화, 피해 0 폭발은 연출 전용 |
| 0.5.1 | 험비 체감 보강: 출발, 오르막, 돌진마다 엔진 레브(HumveeRev, 엔진 루프 두 개로 합성)와 배기 매연, 스로틀 따라 엔진음 피치 상승. 험비 경고는 지상 스타일(주황 CONTACT 배너, 해저드 테이프, 2톤 클랙슨 GroundWarning, 주황 텔레그래프, TOW 추적음 낮춤). `ApacheTelegraph.Spawn`과 `ApacheWarningSystem.Trigger`에 `WarningStyle` 인자 추가(ai[0] = style*64 + mode*8 + banner). 서스펜션 연출(출발 스쿼트, 제동 노즈다이브, 착지 바운스), 화면 흔들림(돌진 통과, 착지, 유탄, 최종 폭발). tML이 빌드할 때 en-US hjson을 자기 형식으로 다시 쓰므로 수정은 기기 파일 기준으로 할 것 |
| 0.5.2 | 험비 패턴 확장(`HumveeBoss.Patterns.cs`): M2 제압 사격 스윕(부채꼴, 트럭 앞 안전지대), Mk19 워킹 탄막(지면 따라 행진), 2페이즈 문짝 없을 때 승무원 M67 수류탄(튕김, 발판 착지, 퓨즈), 2페이즈 박격포 화력 요청(협차 2발 후 효력사, 지붕이 막아 줌, 높은 곳 캠핑 시 가중치 상승). TOW 전용 스프라이트와 비행(튜브 사출 후 측면 노즐 점화, 꼬리 비컨, 유선 유도선 그리기, 트럭이 죽으면 선이 끊겨 직진). 험비 경보음을 2톤 기계식 사이렌으로 교체(후보 A 무전 톤, C 금관 스팅어는 snd/out에 있음). 텔레그래프 패킹 변경: ai[0] = style*256 + mode*16 + banner, WarningBanner.Mortar 추가 |
| 0.5.3 | CH-47D 치누크(`HumveeChinook.cs`, 도트 656x262 좌향, 설계 격자 1.3배, Apache 팔레트, 패널라인/리벳/웨더링). 첫 등장은 치누크 강습 공수 + 컷신(`Common/Systems/HumveeCutscene.cs`: 레터박스, 카메라 추적, 타이틀 카드, 플레이어 잠금/무적). 험비 기어오르기 제거: 높은 곳/벽에 막히면 `HumveeBoss.Airlift.cs`가 치누크 호출, 훅업 후 플레이어 층에 내려놓음(주황 HOSTILE AIRLIFT 표시, 매달린 동안 M2 사격, 슬링 진자 흔들림). 조명은 가산 블렌드 글로우(항법등 깜빡임, 충돌방지등 더블 플래시, 꼬리등, 서치라이트 빔), 로터 루프 2개(앞/뒤 허브 위치, 피치 차) + 터빈. 험비 경보음은 A(무전 톤). 주의: device_commit_files가 같은 outputs 경로를 재사용하면 옛 내용이 써질 때가 있었음 → 커밋마다 새 outputs 폴더를 쓰고 커밋 후 md5 확인 |
| 0.5.4 | 치누크 적재 플랫폼(올리브 적재판 `_Platform`, 208x18)과 실제식 리깅: 리치 펜던트, 에이펙스 피팅(`_Apex`), 슬링 다리 4줄(끝단 체인, 앞 2줄은 험비 PostDraw에서 그려 험비 앞을 지나감), 데크-바퀴 허브 타이다운 4줄(래칫). 진자 흔들림은 가속도 클램프 ±0.8, 이득 0.5로 순화. 공수 흐름: 빈 판 내려놓기 → 험비 자주 탑승 → 타이다운 → 들어올림 → 투하 → 스트랩 해제 → 험비 하차(driveOff 50틱) → 빈 판 달고 이탈. 플랫폼 위에서는 험비가 데크 높이만큼 그려지고 경사로에서 기울어짐(PlatformLiftAt). 오프라인 GIF 렌더러는 클라우드 /home/claude/gif/render.py |
| 0.5.5 | 강습 공수를 다시 만듦. 치누크가 한 번의 긴 하강 진입(5차 에르미트 곡선, 끝 가속도 0)으로 들어와 이동 중에 판을 땅에 닿게 하고, 닿는 순간 타이다운이 풀리며 치누크는 플레어함. 험비는 받은 속도(`ReleaseVx`) 그대로 데크 앞으로 굴러 내려가 시동이 꺼진 채 타력으로 달리다가 브레이크를 밟고 섬(`Coast`). 그다음 냉간 시동을 걸음(`Crank`, `HumveeBoss.Startup.cs`): 스타터가 돌 때마다 헤드라이트가 처지고, 시동이 걸리면 검은 연기와 불꽃이 나오고 조명이 켜짐. 타이틀 카드는 시동이 걸리는 순간(`CatchAge`)에 뜸. 착지점은 `Runway()`로 평지를 찾아 정하고, 굴러갈 거리에 맞는 속도는 `RollInSpeed()`로 계산해서 험비가 FindDrop 지점에 서게 함. 헤드라이트, 측면 표시등, 미등(제동 시 밝아짐)은 가산 글로우로 그림. 시동음 `HumveeStart.ogg`는 합성(스타터)과 kyles 녹음(시동 걸림)을 섞음. 호위 아파치는 사격을 빼고 배경 비행으로 바꿈(`HumveeEscort.cs`: 0.6배, 하늘색 헤이즈, 타일 뒤). GIF 렌더러는 클라우드 /home/claude/gif/render3.py |
| 0.5.6 | 아파치 등장 컷신(`ApacheBoss.Cinematic.cs`, `Common/Systems/ApacheCutscene.cs`)을 새로 넣음. 한쪽 멀리 높은 곳에서 기수를 숙이고 급강하해 들어온 뒤 플레어로 호버에 들어감(5차 에르미트 경로, `CinePath`). 그다음 기관포를 돌리고 TADS 레이저 조준, 락 톤, 타이틀 순서로 나옴. 전투는 Intro 상태의 Timer 50부터 이어짐. 컷신 테두리는 검은 레터박스 대신 경보 스타일 띠(`CutsceneDraw.AlertFrame`)로 바꿈. 음영을 넣은 사선 줄무늬 타일은 `CutsceneStripes.png` 32x14이고, 라벨 판과 T+ 시계가 붙음. 공중은 빨강, 지상은 주황. 모드 설정 `VisualConfig.BlackLetterbox`로 검은 띠를 다시 고를 수 있음. 험비 강습의 호위 아파치는 사격을 빼고 배경 비행으로 바꿈. 아파치 항법등과 비행운은 `ApacheBoss.Lights.cs`에 있음(날개끝 위치등, 꼬리등, 충돌방지등, 로터 끝과 날개끝 비행운). 무기 VFX는 `Common/Visuals/WeaponFx.cs`에 있고 알파 0 가산 트릭이라 배치 전환이 없음. 히드라와 헬파이어는 연기 리본과 배기 글로우, 12.7mm는 빨간 예광, 30mm는 HEDP 탄착 폭발이 들어감. 기기 쪽 PNG는 재인코딩되어 md5가 다르므로 픽셀 해시로 확인할 것. `ApacheBoss_Ping.png`는 지금 쓰지 않음(삭제 권한이 없어 남겨 둠). GIF 렌더러는 /home/claude/gif/render_apache.py, gifhud.py 루프 사운드(로터, 엔진, 터빈)는 이제 전부 `Common/LoopSounds.cs`가 소유함. 소유자가 사라지면 다음 틱에 멈추고, 월드를 나갈 때 전부 멈춤. 콜백이 예외를 던지면 사운드 플레이어가 멈추지 않은 채 목록에서만 빼 버려서 루프가 영원히 남는 문제가 있었고, 이것도 막음. 새 루프는 `LoopSounds.Keep(owner, channel, ...)`로 걸 것. 아파치 비행운은 비행 내내 옅게 깔리고(`VaporFloor` 0.4), 플레어 때 나오던 수증기 폭발은 뺐음. HUD 크기 문제도 고침. 컷신 테두리, 타이틀, 경보 배너는 이제 InterfaceScaleType.None 레이어에서 높이 1080짜리 가상 캔버스(`CutsceneDraw.BeginCanvas`/`EndCanvas`, 폭은 화면 비율을 따름)에 배치하고 실제 백버퍼 크기로 스케일함. 예전에는 UI 배율로 나눈 좌표를 썼는데 레이어에 UI 행렬이 적용되지 않아서, FHD에서 띠가 화면 폭의 91%까지만 그려지고 아래 띠가 화면 중간에 떠 있었음(사용자 스크린샷). |
| 0.5.6 이후 (모드 코드 변경 없음) | AH-64E 가디언 스프라이트를 `apache_e_sprites\`에 만듦. D 도트에 저시인성 회색, 어두운 롱보우 돔, 블랙홀 배기 커버, 라운델과 "U.S. ARMY", 스파이크 NLOS 2단 런처와 앞머리 레이더 포드, 윙팁 MAW 포드(모양 미확정), 꼬리 기어박스 불룩함, 끝이 꺾인 로터. 스파이크 미사일 스프라이트 2장, 등장과 스파이크 발사 GIF. 나셀은 사용자 방침대로 D 그대로 |
| 0.5.7 | **AH-64E 가디언 보스(코드)**: `ApacheGuardian : ApacheBoss`(텍스처만 바꾼 하위 클래스, 로직은 `ApacheBoss.Guardian.cs`의 `IsGuardian` 분기). 5페이즈(임계 1/0.75/0.5/0.3/0.12), 히드라 대신 `SpikeSalvo`(스파이크 NLOS `SpikeNLOS.cs`: 냉간 사출 10틱, 로프트, 급강하, 종말 선회율 상한). 드론 `Content/NPCs/Guardian/`: `UavBase`(고정익 비행, 컷신 스쳐 지나가기 경로, 프롭, NavLightRig), `ShadowUAV`(플레이어 위 선회, 2.5초(4페이즈부터 2초) 레이저 락 → 가디언 연계 스파이크 2~3발 유도 1.8배, 시야가 끊기거나 8% 피해면 락 해제, 주기적 돌격), `GrayEagleUAV`(플레이어 반대편 레이스트랙, 락 표시 후 헬파이어 1~2발). 보너스: 이글 생존 시 가디언 받는 피해 -12%, 섀도 생존 시 속도 +5%와 연계 사격. 소환은 `/apache guardian`(`e`, `64e`). 미사일 연기를 독립 파티클(`Common/Visuals/SmokeTrails.cs`)로 바꿔 발사기 쪽부터 흩어지고 미사일이 사라져도 남음, 발사 시 후폭풍 연기(`WeaponFx.LaunchBlast`). GIF `apache_e_sprites/apache_guardian.gif` |
| 0.5.8 | 가디언 회피 360도 루프(`AttackState.Evade`, 60틱 안에 체력 2.5% 넘게 잃으면 발동, 2페이즈부터, 쿨다운 600틱, 도는 동안 받는 피해 ×0.4, 시작할 때 플레어). 드론 등장 스윕 패스를 v4 GIF 방식으로(드론마다 `PassHeight`, `PassSpeed`, `PassDelay`). 그레이 이글 HP 9000, 섀도 락이 끝나면 `Cue()`로 같이 헬파이어. RQ-7 섀도를 1:1 픽셀 76×34로 새로 그림(HP 3000, 판정 70×26, `NavLightRig.RQ7Shadow` 오프셋 1:1 기준, 렌즈 0.6). 등장 GIF v5(`entrance_gif_v5/`). 자세한 건 `10_v0.5.8_이어받기.md` |
| 0.5.9 | 가디언 전용 보스 음악(`GuardianTheme.ogg`, 사용자 자작곡, 루프 태그). 드론 엔진 루프(그레이 이글 프롭 엔진, 섀도 윙윙 모터)가 드론을 따라다니고, 등장 컷신 스쳐 지나가기에 각 드론의 통과음이 가디언 옆을 지나는 순간에 맞춰 나옴. `ModSounds`에 4개 등록, `UavBase`에 `EngineLoop`/`PassSound`/`PassPeak`. 컴파일 에러 0 |
| 0.5.10 | 가디언 돌진 강화: 가방의 약 40%가 돌진(Ram/LongCharge), 가까우면 공격 사이에 스냅 돌진(`TrySnapRam`, 쿨다운 7초에서 3.7초, 60%), 짧은 돌진은 준비 34틱, 속도 ×1.15, 최소 2회, 로켓 대신 기관포. 가디언은 히드라와 헬파이어를 직접 안 쏨: 스탠드오프, 출격(상공 웨이브와 일제사격), 레이더 락, 팝업, 로프트 헬파이어 자리가 전부 `FireSpike`로 바뀜. 헬파이어는 그레이 이글만 쏘니까 이글이 떨어지면 끊김. 스파이크 일제사격 3/3/4/4/5발. 드론: `chaseable = false`(유도 무기와 소환수가 안 노림, 직접 조준해야 함 — 엔드게임 유도 장비에 몇 초 만에 녹아서 "사라지는" 것처럼 보였음), 격추되면 불붙어 추락하다 땅에서 폭발(`CheckDead`/`DownedAI`, `IsDowned`면 보너스와 연계에서 빠짐). 가디언 컷신 HUD: `WarningStyle.Guardian`(청록, 깎은 모서리 판, 쉐브론, 다이아몬드 락 아이콘, `TARGETED`), 진입 70틱에 `WarningBanner.Targeted` 배너, 타이틀은 `CutsceneDraw.TitleLeft`(왼쪽 아래). 번역 키 `Warnings.HeaderGuardian`, `Warnings.Targeted` 추가. 컴파일 에러 0 |
| 0.5.11 | 가디언 등장 컷신을 v5 흐름으로 교체(`ApacheBoss.GuardianCinematic.cs`): 옆쪽 2680px 밖에서 활공 진입(0~200틱), 128틱 FCR 지정과 `TARGETED` 배너, 214틱 스파이크 발사(데미지 0), `ApacheCutscene`이 미사일 추적 카메라, 640px 안에서 시커 화면(줌 2/3/4배, 스캔라인, 조준선, RNG/TTI), 명중 시 화이트아웃 후 40틱 유지, 가디언으로 휩 팬, 424틱 드론 통과, 488틱 청록 타이틀, 610틱 전투 시작. 스파이크 날개 전개 스프라이트 `SpikeNLOS_Deploy.png`(5프레임 40x34) 인게임 적용. 회피 360도 루프 반경 150에서 320, 58틱에서 90틱. 항법등, 비콘, 엔진 글로우가 낮에는 어두워짐(`NavLightRig.Lens`/`Dim`, 아파치, 가디언, 치누크, 호위, 험비). 컴파일 에러 0 |
| 0.5.12 | 가디언 버그: 6000px만 멀어져도 이탈 후 10틱 만에 삭제되던 것을 플레이어 사망이나 9000px 밖 10초일 때만으로 바꿈(`farTicks`), `CheckActive` 막음. 등장 스파이크는 `SpikeNLOS.CineMark`(ai[2]≥50)로 표시해서 정점까지 올라간 뒤 급선회해 플레이어 위에서 터짐. 가디언 전투 경보, 조준 표시, 락 마크를 청록 `WarningStyle.Guardian`으로, 2페이즈 로그는 `GuardianPhase2`. 신규: Mk19(험비 20%), M230(아파치 D 20%), 그레이 이글 단말기(가디언 확정, 실물 크기 소환수 2칸), IHADSS 헬멧(가디언 1/3) 탈것 흐름(치누크가 마스트 인양고리로 슬링 수송해서 앞에 내려놓고 이탈, 캐노피 열고 탑승, 하차하면 그 자리에 주기. 정비반과 착륙·램프 연출은 사용자 요청으로 뺌), 가디언 HUD(`GuardianHud`), MUM-T 데이터링크 단말기(가디언 소환템, `downedGuardian`). 로어: 회수 기록 3종(Shift로 읽기, `Items.*.LoreText`), 도감 한 줄, 툴팁 한 줄. 편대등 LED 바 분할. 컴파일 에러 0 |
| 0.5.12 이후 (같은 버전) | 가디언 수송 치누크를 가디언 축척으로 새로 뽑음(`Content/Projectiles/Mounts/GuardianChinook*.png`, 험비전 텍스처는 그대로). 동체 0.583배를 레이어 축소(`chinook_small2.py`: 최다 색 면, 1px 선 따로 옮김, 현창과 "U.S. ARMY" 다시 찍음)로, 로터는 따로 아파치 로터의 1.25배 342 px(CH-47 18.29 m, AH-64 14.63 m). 슬링 줄 120에서 70. 그레이 이글 헬파이어를 AGM-114R로 분리(`GrayEagleHellfire`, `FriendlyEagleHellfire`, 비행은 같고 이름만 다름). 인수인계 정리: 이 모드 인수인계는 `E:\Downloads\ModernArsenal_인수인계\` 하나로 합침(00을 0.5.12 기준으로 다시 씀, 폴더 위치 표와 세션 시작 순서, `compile_check/` 추가, 옛 `ModernArsenal_인수인계_1`, `_v0.5.6`은 지움). 다른 모드 만들 때 쓸 인수인계는 별도 폴더 `E:\Downloads\테라리아_새모드_인수인계\`. 컴파일 에러 0 |
| 0.4.2 | 무전기를 쓰면 화력 지원 단말기가 열리고 송신 버튼을 눌러 호출. M4A1 실제 녹음 소리(사격 4종, 조정간, 탄피 4종), 밸런스 조정 (피해 11, AUTO 탄 절약 25%, SEMI 1.8배와 치명타 +10%와 방어 관통 10). `/m4a1`, `/apache m4` 명령어 |

...여기까지 읽었으면 대충 다 알 거예요. 모르겠으면 02번 파일 5-3부터 다시 읽어요.
