# v0.5.8 (2026-09-23): 이전 세션이 인수인계 문서를 쓴 뒤에 추가로 작업한 내용

기기의 ModSources\ModernArsenal에는 이미 반영되어 있어요. 컴파일 확인과 md5, 픽셀 해시 검증까지 마쳤어요.
build.txt는 0.5.8이고, description.txt에 v0.5.8 changelog가 들어가 있어요.

## 바뀐 파일
- Content/NPCs/Apache/ApacheBoss.AI.cs: AttackState에 `Evade = 19`를 추가했고, dispatch에 DoEvade를 연결했어요.
- Content/NPCs/Apache/ApacheBoss.Guardian.cs
  - Evade는 360도 루프 회피예요. EvadeTicks 58, 반지름 150, smoothstep 각도를 쓰고, 시작할 때 플레어를 뿌려요. 루프 도는 동안 받는 피해는 x0.4예요.
  - 반응형 발동: 60틱 안에 maxLife의 2.5%를 넘게 잃으면 발동해요. 조건은 Phase 2 이상, 쿨다운 600틱이 끝났을 때, 그리고 `CanEvadeNow()`가 참일 때예요(Reposition, GunStrafe, HellfireVolley, SpikeSalvo, Standoff 상태).
  - 가방 가중치: 페이즈 3과 4에서 1, 페이즈 5에서 2예요.
- Content/NPCs/Guardian/UavBase.cs
  - 등장 연출을 v4 GIF 방식 스윕 패스로 바꿨어요. PassFrom 108, PassStart 1200이고, 드론마다 PassHeight, PassSpeed, PassDelay를 따로 줘요.
  - PropFrameGap을 추가했어요. 거리가 1200을 넘으면 FlyTo가 가속해요.
- GrayEagleUAV.cs: HP 9000이고, 패스 값은 -110/19/0이에요. `Cue()`가 호출되면 섀도우 락이 끝날 때 같은 레이즈로 헬파이어를 쏴요.
- ShadowUAV.cs: HP 3000이고, 패스 값은 95/16/12예요. 히트박스는 70x26이고, 센서 위치는 (-18.5,5.5), PropAxis는 (2.5,-2.5), 프로펠러 프레임은 높이 13, 간격 2예요.
- Common/Visuals/NavLightRig.cs: RQ7Shadow 오프셋을 1:1 스프라이트 기준으로 바꿨고, Size는 0.6이에요.
- ShadowUAV*.png: 아파치의 약 1/5 크기로 줄이면서 디테일은 유지하려고 **2배율이 아니라 1:1 픽셀**(76x34)로 그렸어요. 스크립트는 이 폴더의 build_shadow_fine.py예요(SX 24.5, SY 14.5). 예전 rq7_shadow_sprites 폴더에 있는 큰 버전은 이제 쓰지 않아요.

## 등장 GIF v5 (entrance_gif_v5/)
- render_apache_e_v5.py는 사용자가 준 v4 스크립트를 바탕으로 해요. placeholder 박스를 실제 그레이 이글과 섀도우 스프라이트로 바꾸고, 프로펠러와 항법등을 넣었어요.
- 연기는 퍼프 하나하나가 수명 110~240틱을 따로 가지고 (1-u)^2로 옅어져요. 발사관 쪽부터 사라지게 하려는 친구 피드백 반영이에요.
- 발사할 때 발사관 뒤로 백블라스트 구름이 터져요(친구 피드백).
- 1인칭 시커 화면은 전보다 짧게 줄였어요(거리 1050에서 640으로).
- SpikeNLOS_Deploy.png는 v4 원본 시트를 찾지 못해서 spike_sprite.py로 다시 만든 거예요. 원본이 나오면 바꿔 끼우면 돼요.
- assets 폴더는 apache_e_sprites와 gray_eagle_sprites, 그리고 이 폴더의 ShadowUAV 파일들에 CutsceneStripes.png와 HumveeChinook_Glow.png를 더해서 구성해요.

## 아직 안 한 것 (2026-09-24 갱신)
- (2026-09-24) 이 문서 내용은 03_도트_스프라이트.md 8절(섀도)과 08_변경이력.md 0.5.8 줄에 옮겨 넣었어요.
- 인게임 테스트를 아직 못 했어요. Evade 루프 모양, 드론 크기와 등장 패스를 실제로 확인해야 해요.
