# ModernArsenal 인수인계 09: 외부 조언 (커뮤니티 피드백)

> 여러 파일로 나눠 둔 인수인계서 중 하나예요. 전체 목록은 `00_먼저_읽기.md`에 있어요.
> 사용자가 커뮤니티에서 받아 온 조언을 모아 둔 파일이에요. 원문은 그대로 두고, 적용할 때 필요한 메모를 붙였어요.
> **v0.4.4에서 세 가지 모두 코드에 적용했어요.** 교란 NPC는 `Content/NPCs/Shared/DecoyFlare.cs`, 연기 고어는 `Common/Bosses/BossFx.cs`(`ExplosionBase` 첫 틱과 파츠 파괴 때 호출), 부위 공통화는 `Common/Bosses/BossParts.cs`(`IPartOwner`, `BossPart`, `PartSet`)예요. 아파치는 이 틀로 옮겼고 컴파일 에러 0, 경고 0이에요. 게임 안 확인은 아직이에요.

---

## 1. 교란 NPC 패턴 (아파치 플레어)

**원문 요지:** 교란장치 패턴을 만들 때 투사체 말고 적 판정인 NPC를 뿌린다. 그러면 플레이어가 쏘는 유도 투사체가 엉뚱한 표적을 잡는다. 무더기로 뿌리면 비유도 투사체도 막혀서 정말 거슬리는 패턴이 된다.

**사용자 판단:** 좋다고 함. 채택 예정.

**적용 메모**

- 지금 `ApacheFlare`는 `hostile`과 `friendly`가 둘 다 꺼진 연출용 투사체예요. 플레어의 원래 역할이 유도 무기를 속이는 거라, 이걸 NPC로 바꾸면 고증에도 맞아요.
- 유도 투사체가 잡으려면 NPC가 `CanBeChasedBy()`를 통과해야 해요. `friendly`가 꺼져 있어야 하고, `lifeMax`가 5보다 커야 하고, `dontTakeDamage`와 `immortal`도 꺼져 있어야 해요. 바닐라 유도와 칼라미티 유도 무기 대부분이 이 함수로 가장 가까운 적을 골라요. 소환수도 똑같이 낚여요.
- **흡혈 파밍을 막아야 해요.** 체력은 낮게, 수명은 2~3초로 짧게 잡아요. 드랍, 코인, 배너, 도감 등록은 전부 꺼요. 피해 숫자는 `modifiers.HideCombatText()`로 숨겨요.
- NPC 슬롯은 200개가 전부이고 보스 파츠도 NPC예요. 한 번에 20~30개까지만 뿌려요.
- 서버에서 스폰하면 멀티 동기화는 기본으로 따라와요.
- 배치 안: 긴 돌진 때 뿌리는 플레어를 이걸로 바꾸고, 3페이즈부터 대량으로 뿌리는 전용 패턴을 따로 둬요. 출격 중에 상공에서 뿌리는 것도 어울려요.

---

## 2. 폭발에 바닐라 연기 고어 (61, 62, 63)

**원문:** 폭발 이펙트가 지금도 예쁘긴 한데 테라리아답지는 않다. 테라리아 고어 입자 중 하나인 스모크를 같이 뱉어주는 걸 추천한다. 아이디는 61, 62, 63. 예시 코드:

```csharp
int type = Main.rand.Next(61, 64);
int smoke = Gore.NewGore(Projectile.GetSource_Death(), source, default, type, 1f);
Gore gore = Main.gore[smoke];

Vector2 goreDir = Main.rand.NextVector2Unit();
gore.velocity = goreDir * (2.5f * velocityMult);
```

**사용자 판단:** 좋다고 함.

**적용 메모**

- 61~63은 `GoreID.Smoke1`~`Smoke3`이에요. 바닐라 폭탄과 로켓이 터질 때 쓰는 그 연기예요.
- 넣을 자리는 `ExplosionBase.AI()`에서 폭발음을 한 번 재생하는 첫 틱이에요. 그러면 히드라, 헬파이어, 아군 폭발에 한 번에 다 들어가요.
- 예시 코드는 그대로 붙이면 안 돼요. 폭발 투사체는 죽으면서 뿜는 게 아니라서 `GetSource_Death()` 대신 `Projectile.GetSource_FromThis()`를 써요. `source`와 `velocityMult`는 원래 코드의 변수라 여기엔 없어요. 위치는 `Projectile.Center`에서 24px씩 빼요. 연기 스프라이트가 48px쯤이라 바닐라도 좌상단 기준으로 24를 빼요.
- 개수는 폭발 크기에 비례하게 해요. 히드라는 4~5개, 헬파이어는 7~8개가 적당해요. 히드라 38발이 한꺼번에 터지면 고어가 200개 가까이 되니까, 동시에 많이 터질 때는 줄이는 처리를 같이 넣어요.
- 전용 서버에서는 고어를 만들지 않게 `Main.dedServ`로 막아요.
- 연기 고어는 월드 조명을 받아서 밤이나 동굴에서는 어둡게 보여요. 그게 테라리아다운 부분이에요.

---

## 3. 공통 컴포넌트화

**원문:** 앞으로 보스들 부위 만들 거면 반드시 공통 컴포넌트화 해두는 걸 추천해... 부위 시스템뿐만 아니라 전반적으로 설계할 때 가능한 한 공통화 안 해놓으면 기술부채 지옥이 찾아온다... 경험담임.

**사용자 판단:** 이런 방향으로 가야 함.

**현재 상태 (아파치 전용으로 박혀 있는 것)**

- `ApacheParts.cs`: `ApachePartKind` 열거형, `ApachePart : ModNPC`와 부위별 하위 클래스 네 개.
- `ApacheBoss.cs` 160~250행 부근: `DestroyedParts`(부위마다 비트 하나인 byte), `SpawnParts()`, `PartCenter()`의 부위별 switch, `RefreshPartCache()`, `PartCovers()`(파츠가 본체 피격을 가리는 판정), `OnPartDestroyed()`(시스템 로그 키가 `"PartLost" + kind`).
- 이 구조로 보스 16마리를 만들면 부위 코드가 보스마다 복사돼요.

**뽑아낼 공용 틀 (험비 들어가기 전에)**

- **부위:** 모든 보스가 쓰는 `BossPart` 기반 클래스(`ai[0]` 주인, `ai[1]` 부위 번호). 주인 보스가 구현하는 `IPartOwner`(부위 개수, 부위 월드 위치, 부위 체력 비율, 파괴 콜백). 비트마스크, 캐시, 스폰, 피격 가림 판정, 동기화를 한 곳에서 처리하는 `PartSet` 도우미.
- **07번 로드맵에 이미 적힌 공용 목록:** 파츠와 불량 모드, 경고선과 WARNING 배너, 영어 무전과 시스템 로그, 손상 단계 연출, 격추 연출, 출격(상공 이탈) 구조. 여기에 위 1번(교란 NPC)과 2번(폭발 + 연기 고어)을 더해요.
- **분리되는 부위 그리기:** 험비 스프라이트는 이미 문, 보닛, 포탑, 바퀴 같은 레이어로 나눠 뒀어요 (`03_도트_스프라이트.md` 5장). 레이어 이름을 부위 번호에 대응시키는 공용 규약으로 만들면, 부위가 부서질 때 그 레이어를 빼고 같은 그림을 고어로 날리는 처리를 롱보우 편대, 브래들리, 에이브람스에서 그대로 재사용해요.
- **순서 제안:** 공용 틀을 뽑아요. 아파치를 거기에 옮겨서 동작이 똑같은지 먼저 확인해요. 그다음에 험비를 만들어요.
